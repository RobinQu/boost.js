define(["core"], function(boost) {
  
  // Binding
  // Binding.from(obj1, "foo.bar").to(obj2, "foo.bar");
  
  var Binding = boost.Object.extend({
    
    
    
  });
  
  Binding.oneway = function(obj, path) {};
  
  
  
  return Binding;
  
});